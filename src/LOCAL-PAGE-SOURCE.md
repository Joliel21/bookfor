# Local Magazine Page Source

The RARE Revolution Magazine pages in this build are bundled locally under:

`public/images/rare-pages/`

The reader no longer requests the cover, inside covers, numbered pages, or back cover from GitHub. This removes the GitHub Raw loading delay.
